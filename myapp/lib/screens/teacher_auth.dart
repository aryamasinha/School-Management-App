import 'package:flutter/material.dart';

class TeacherAuth extends StatelessWidget {
  static const routeName = '/teacher-auth';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(child: Text('inside teacher auth'),));
  }
}
