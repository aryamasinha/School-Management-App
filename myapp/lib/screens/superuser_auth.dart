import 'package:flutter/material.dart';

class SuperuserAuth extends StatelessWidget {
  static const routeName = '/superuser-auth';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(child: Text('inside principal auth'),));
  }
}
